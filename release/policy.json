{
  "_note": [
    "The committed, machine-readable release policy for this application. Every field",
    "here is READ AND VALIDATED by release/lib/policy.mjs before release/lib/decide.mjs",
    "evaluates anything, and every field is decision-bearing: an invalid policy is one",
    "refusal (policy.invalid) naming every problem. release/README.md explains the",
    "reasoning for a human and is documentation, NOT the gate (D08-A2).",
    "",
    "The gate and the publisher read THIS FILE FROM THE DEFAULT BRANCH, never from the",
    "candidate: a candidate that supplied its own policy could relax its own gate. The",
    "publisher additionally runs the verifier pinned to the exact revision the gate ran",
    "and refuses (preflight.policy_advanced) if release/ on the default branch has moved",
    "since, so changing anything here takes effect only on a fresh publication run."
  ],
  "schema": "dinify.release.policy/2",
  "application": "dinify-frontend",
  "repository": "mugak1/Dinify-Frontend",
  "defaultBranch": "main",
  "certification": {
    "_note": [
      "The certifying workflow is resolved by PATH, through the GitHub API, and only",
      "then compared with what the triggering event claimed. `workflow_run.workflows`",
      "can match a workflow's mutable DISPLAY NAME and nothing else, so the name in",
      "publish.yml is a coarse pre-filter GitHub forces on us — never the proof.",
      "",
      "requiredChecks names the jobs inside that workflow whose success IS the",
      "certification. A missing check is refused exactly as a failed one is."
    ],
    "workflowPath": ".github/workflows/certify.yml",
    "event": "push",
    "branch": "main",
    "requiredChecks": [
      "certify"
    ]
  },
  "freshness": {
    "_note": [
      "A certification describes a moment. A candidate older than this window is not",
      "promoted on its old statement; it is re-certified.",
      "",
      "THIS IS A CONSERVATIVE LIMIT, NOT AUDIT EVIDENCE. Nothing in the gate proves the",
      "certified dependency set was audited within this window: the candidate-inventory",
      "rescan is B2, and has not been supplied. The window is kept as the limit until that",
      "rescan is wired, and the certified payload is never rebuilt as a substitute for",
      "scanning it."
    ],
    "certificationWindowHours": 24
  },
  "build": {
    "_note": [
      "THE configuration that ships. CI and the certifying run build this and nothing",
      "else, and the publisher refuses an artifact stamped with any other name.",
      "",
      "`uat` is the honest name for it: it carries the UAT-targeted API origin, which",
      "is the destination this stack has today and which Stage B deliberately does NOT",
      "change. It is not 'production' and this file does not call it that. The",
      "environment it bakes sets Angular's `production` flag FALSE — recorded in every",
      "manifest as environment.productionFlag so the diagnostic behaviour that follows",
      "from it is a written-down fact rather than a discovery.",
      "",
      "expectedApiUrl and forbiddenOriginsInBundle are asserted against the BUILT",
      "BYTES by `release/cli.mjs stamp`, not against the source. A file replacement",
      "that silently failed would otherwise produce an artifact that passes every",
      "check and points at the wrong API."
    ],
    "configuration": "uat",
    "expectedApiUrl": "https://api-test.dinifyapp.com/uat",
    "expectedProductionFlag": false,
    "forbiddenOriginsInBundle": [
      "https://api.dinifyapp.com"
    ]
  },
  "hosting": {
    "_note": [
      "The destination, pinned — and the ONLY configuration the publish tool is given.",
      "The gate reads firebase.json AND .firebaserc at the certified commit, resolves the",
      "destination the way firebase-tools does (release/lib/hosting.mjs), refuses anything",
      "outside a narrow allow-list, proves every certified file survives the effective",
      "ignore set, and the publisher then hands the tool a REGENERATED pair built from",
      "these values. Its digest is part of the admitted record.",
      "",
      "ignore must equal the certified firebase.json entry exactly; a change there is a",
      "reviewed change to this file, never a silent override. publicDirectory is where",
      "the verified payload is staged beside the regenerated configuration.",
      "",
      "identityOrigin is the canonical Firebase site origin for this target and is where",
      "the served identity is read back from. It is NOT a claim that it is the only",
      "origin serving this content: which custom domains are mapped, and how a printed",
      "diner QR URL stays valid, is an owner inventory item. Nothing here redirects,",
      "reroutes or reprints anything.",
      "",
      "firebaseToolsVersion pins the tool the publisher runs. The oracle test reads the",
      "version installed in this repository, which may differ; that difference is",
      "recorded by release/tests/hosting-oracle.test.mjs rather than assumed away.",
      "",
      "verification is how long the publisher waits for the origin to serve the admitted",
      "identity after the tool returns: `attempts` reads, the n-th after n x intervalMs.",
      "Only the WAIT is configurable — what counts as verified is not."
    ],
    "project": "dinify-dev",
    "site": "dinify-prod",
    "target": "dinify-prod",
    "publicDirectory": "./dist",
    "channelId": "live",
    "identityOrigin": "https://dinify-prod.web.app",
    "identityPath": "/release.json",
    "firebaseToolsVersion": "15.30.2",
    "verification": {
      "attempts": 5,
      "intervalMs": 5000
    },
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ]
  },
  "storage": {
    "_note": [
      "Where the reviewed storage compatibility declaration lives. The gate reads it",
      "from git at the certified commit (and at a bootstrap baseline), never from the",
      "candidate upload. See the declaration file and release/lib/storage.mjs."
    ],
    "declarationPath": "src/app/_services/checkout-record.storage.json"
  },
  "bootstrap": {
    "_note": [
      "Nothing on this path has ever published an identity file, so the first",
      "publication has nothing to order itself against and no served storage",
      "declaration to be compatible with. Both are supplied by an EXPLICIT, reviewed",
      "owner decision: authorized true AND servedBaseline naming the commit whose build",
      "is live (its storage declaration is then read from git). Until both are set, a",
      "candidate the gate cannot place in order is refused (served.bootstrap_unauthorized,",
      "storage.bootstrap_baseline_missing).",
      "",
      "This is the frontend naming ITS OWN baseline; it is not serving evidence for a",
      "peer. The same pull request that authorizes it should revert it once an identity",
      "is served."
    ],
    "authorized": false,
    "servedBaseline": null
  },
  "eligibility": {
    "_note": [
      "Eligibility is SEPARATE FROM ANCESTRY. An ordinary revert leaves the reverted",
      "commit an ancestor of main for ever, so ancestry can never mean \"a target anybody",
      "still wants\". A revoked commit is never promoted, by any path; a minimum safe",
      "target is the floor below which nothing is promoted. Both are applied on every",
      "promoting path, rollback included."
    ],
    "minimumSafeTarget": null,
    "revoked": []
  },
  "compatibleSet": {
    "_note": [
      "The approved combination, as EVIDENCE rather than literals. Each approved peer",
      "revision is a RECEIPT under release/peers/, produced from that peer's own git by",
      "node release/cli.mjs peer-receipt and pinned here by digest. The receipt's",
      "contract values and capability levels are READ FROM THE PEER'S EXPORT FILES at",
      "that revision; nothing in this file restates them.",
      "",
      "SELECTION IS NOT SERVING. A receipt proves what a revision's SOURCE says; it",
      "does not prove that revision is live. Serving is observed separately where a peer",
      "publishes an identity, and refused by name where it does not.",
      "",
      "CROSS-REPOSITORY CHANGES REMAIN AN ORDERED, MANUAL SEQUENCE. This gate is the only",
      "path that consults this file: the backend's own deploy and the legacy",
      "deploy-prod.yml make no compatibility decision. A new peer revision needs a new",
      "receipt approved here in a reviewed pull request; an unchanged peer stays pinned.",
      "",
      "The frontend's own commit is deliberately not pinned: a file cannot contain the",
      "SHA of the commit that introduces it."
    ],
    "id": "2026-09-24-pilot-4",
    "frontendRequires": {
      "buildConfiguration": "uat"
    },
    "peers": {
      "backend": {
        "repository": "mugak1/Dinify-Backend",
        "receiptVerification": "operator",
        "_receiptVerification": "The backend repository is private, so its receipt cannot be re-derived from a public API at decision time. It is produced by an operator from the backend's git and reviewed as a file; its digest is what is approved.",
        "_approvedHistory": "9448f55 (the #330 merge) was the first approved backend revision. It predates orders_app/contracts/published_capabilities.contract.json, so it was REPLACED by 366b7e4 (the #331 merge) rather than kept beside it: every approved backend is checked for compatibility, and an export-less revision left in this list would refuse peers.capabilities_unpublished for ever. 366b7e4 was in turn REPLACED on 2026-09-24 by a6b25a6 (the #338 merge) after review of #332-#338: both export files are byte-identical between the two (D01 blob 1cb6a6d, capability blob cc91605), so the D01 values and the four capability levels are unchanged. Both earlier receipts stay under release/peers/ as history; neither is approved. Approving a receipt is a statement about SOURCE and says nothing about which backend revision is live.",
        "approved": [
          {
            "commit": "a6b25a619d572c8de68964ab9ca4b4c2ae9ebe0b",
            "receipt": "release/peers/backend-a6b25a619d572c8de68964ab9ca4b4c2ae9ebe0b.json",
            "receiptDigest": "sha256:c1355f5059f24506e9637ad29c24e4782a71275bfa5a0b18a80ed07cc5d3b920"
          }
        ],
        "serving": {
          "observation": "unavailable",
          "reason": "the backend publishes no served-revision identity (B3); which backend revision is live cannot be observed, and an operator statement is not accepted as serving evidence"
        }
      },
      "admin": {
        "repository": "mugak1/Dinify-Admin",
        "receiptVerification": "public-repository",
        "_approvedHistory": "38df037 (the Admin #25 merge) was the first approved Admin revision. It was REPLACED on 2026-09-24 by 3521ebd (the Admin #27 merge) after review of the two merges between them: #26 moved the SHA-pinned aws-actions/configure-aws-credentials in deploy.yml from v6.2.4 to v6.3.0 (the receipt-bearing blob, c1a5e7c -> 0e210bf), and #27 changed test-restaurant wording, comments and specs only. 38df037's receipt stays under release/peers/ as history and is no longer approved, so an Admin rollback to it is refused as peers.admin_serving_unapproved until it is approved again in a reviewed change.",
        "approved": [
          {
            "commit": "3521ebd05e5623878d262152bde11734b9cbd4fc",
            "receipt": "release/peers/admin-3521ebd05e5623878d262152bde11734b9cbd4fc.json",
            "receiptDigest": "sha256:e58f7ff5fdd0deb0b21d3b78f8b7fd55f2b56781210f8027a2f640b343debfb1"
          }
        ],
        "serving": {
          "observation": "public-identity",
          "origin": "https://admin.dinifyapp.com",
          "path": "/release.txt"
        },
        "assumptions": [
          "Admin is served from its own origin and shares no runtime API, storage or session with this application.",
          "This application contains no code path that can authenticate an administrator (PR-6); no Admin protocol is assumed.",
          "Admin publishes the exact commit it serves at /release.txt with Cache-Control: no-store, asserted by its own deploy workflow after every promotion."
        ]
      }
    }
  },
  "prerequisites": {
    "_note": [
      "OWNER ACTIONS THE FIRST PUBLICATION ON THIS PATH NEEDS. Each is refused by name",
      "while it is outstanding, so the list of what is missing is always the gate's own",
      "output rather than a recollection.",
      "",
      "sourceProtection: branch protection on the certified branch (required checks,",
      "no force-push) recorded, or its absence disclosed as a limitation.",
      "retention: what Firebase Hosting itself retains for this site — the owner's",
      "fallback if this path cannot publish. A rollback ON this path does not depend on",
      "it: it republishes the target's certified artifact, which the gate requires to be",
      "listed, unexpired and inside the certification window.",
      "singlePublisher: ONE active writer for the site. While deploy-prod.yml publishes",
      "independently, outside this workflow's concurrency group, enabling this path would",
      "give the site two uncoordinated writers. The gate refuses on the policy's statement",
      "AND on the observed presence of the legacy workflow file; the cutover retires it",
      "in the same change that enables this path."
    ],
    "sourceProtection": {
      "status": "unrecorded"
    },
    "retention": {
      "status": "unverified"
    },
    "singlePublisher": {
      "status": "legacy-writer-active",
      "legacyWorkflow": ".github/workflows/deploy-prod.yml"
    }
  },
  "publication": {
    "_note": [
      "PUBLICATION IS DISABLED UNTIL AN OWNER ENABLES IT. The publisher evaluates every",
      "gate, re-checks the admitted unit inside its critical section, and then stops,",
      "because the repository variable named here is not set.",
      "",
      "MERGING CHANGES TO THIS DIRECTORY IS NOT INERT IN EVERY RESPECT, and saying so is",
      "part of the record: the legacy deploy-prod.yml still builds and publishes every",
      "merge to main, and it builds the angular.json configuration named in build above.",
      "What this path does not do is publish.",
      "",
      "Setting the variable is NOT a cutover on its own: with deploy-prod.yml still",
      "publishing, the site would have two independent writers. The cutover retires",
      "deploy-prod.yml and records the prerequisites above in the same reviewed change."
    ],
    "enablementVariable": "FRONTEND_PUBLISH_ENABLED",
    "readiness": {
      "_note": [
        "WHAT A NON-PUBLISHING EVALUATION MAY REPORT AS WAITING RATHER THAN FAILED.",
        "Each entry is a pending commissioning prerequisite this policy itself records",
        "as outstanding. A refusal made up of EXACTLY these, each standing in its",
        "context (release/lib/readiness.mjs), from an AUTOMATIC deploy evaluation with",
        "publication disabled, completes the evaluation without a red run. The DECISION",
        "is unchanged: it is still REFUSE, allow:false, nothing is admitted and nothing",
        "is published. Any other reason, any enabled or manual attempt, and any entry",
        "whose prerequisite this policy records as resolved stays red.",
        "",
        "The change that resolves a prerequisite removes its entry here in the same",
        "reviewed change; committed-policy.test.mjs holds this list equal to what the",
        "committed policy actually refuses."
      ],
      "awaiting": [
        "peers.backend_serving_unverified",
        "prerequisite.legacy_publisher_active",
        "prerequisite.legacy_publisher_present",
        "prerequisite.retention_unverified",
        "prerequisite.source_protection_unrecorded",
        "served.bootstrap_unauthorized"
      ]
    }
  }
}
